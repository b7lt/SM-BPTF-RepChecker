// IF and only IF a deal method is triggered for a user (ex. the user is site-banned or feature-banned),
// the plugin will use the value found here rather than the one set in the ConVar
// if such a value is present.

// Deal Methods:
//      0 - Ignore
//      1 - Tag User & Warn other players
//      2 - Kick User
//      3 - Ban user

"BackpackTF Overrides"
{
    // "76561198073823378"
    // {
    //     "SiteBan"           "0" // IF the user is site-banned, then the deal method is overridden to method 0 (ignored)
    //     "NegativeTrust"     "1" // IF the user has negative trust above threshold, then the deal method is overwritten to method 1 (tag/warn)
    //     "FeatureBan"        "0" // IF the user is feature-banned, then the deal method is overridden to method 0 (ignored)
    // }

    
}